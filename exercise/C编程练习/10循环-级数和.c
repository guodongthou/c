/*计算级数和，如n=10，x=0.3时，值为1.349859。
                 x^2    x^3          x^n
    s = 1 + x + ── + ── + …… + ──
                 2!     3!            n!      */
#include <stdio.h>
#include <math.h>
double fun(double x,int n)
{
    
}
main()
{   
	printf("%f\n",fun(0.3,10));
}
