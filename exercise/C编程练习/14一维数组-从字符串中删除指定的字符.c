/*从字符串中删除指定的字符。*/
#include <stdio.h>
fun(char s[],int c)
{

}
main()
{
   char str[]="turbo c and borland c++";
   char ch;
   printf("输入一个字符：");
   scanf("%c",&ch);
   fun(str,ch);
   puts(str);  
}

