/*请用指针编写函数fun，它的功能是删除字符串中所有的数字字符。
如输入123A4BC5DEF67，输出ABCDEF。*/
#include  <stdio.h>
void fun(char *a)
{
	
}
main()
{
	char  s[81];
	printf("Enter a string:\n");
	gets(s);
	fun(s);
	printf("The string after deleted:\n");
	puts(s);   
}
